pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}

buildCache {
    local {
        directory "${System.getProperty("test.build.cache.dir")}"
    }
}
